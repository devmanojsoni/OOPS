template <class T>
T sub(T x, T y){
    return x-y; 
    // calling the add function from another func
    // to demonstrate the use of globally declared function
}
template <class F>
F add(F x, F y){
    return x+y; 
    // calling the add function from another func
    // to demonstrate the use of globally declared function
}